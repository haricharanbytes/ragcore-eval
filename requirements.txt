# --- Web framework ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17        # required by FastAPI for file uploads (UploadFile)

# --- Config & validation ---
pydantic==2.10.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- RAG / LangChain ---
langchain==0.3.9
langchain-community==0.3.9
langchain-groq==0.2.1            # Groq LLM integration
langchain-huggingface==0.1.2     # HuggingFace embeddings integration
langchain-chroma==0.1.4          # Chroma vector store integration

# --- Embeddings ---
sentence-transformers==3.3.1     # backs the HuggingFace embedding models

# --- Vector store ---
chromadb==0.5.20

# --- Document parsing ---
pypdf==5.1.0                     # PDF text extraction
python-docx==1.1.2               # DOCX text extraction

#reranking
rank_bm25==0.2.2

#Eval
ragas==0.2.10                    # Faithfulness / Answer Relevancy scoring, judged by our own Groq LLM

# --- Metadata storage ---
SQLAlchemy==2.0.36

# --- Testing (dev) ---
pytest==8.3.4
httpx==0.28.0                    # FastAPI's TestClient dependency